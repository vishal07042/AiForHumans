import SystemDesignFlow from '../_components/SystemDesignFlow';

export const metadata = {
  title: 'URL Shortener — System Design Flow',
  description:
    'Interactive AlgoMonster-style flowchart for URL Shortener HLD: unfold requirements, HLD flows, ID generation, encoding and scaling.',
};

export default function UrlShortenerPage() {
  return <SystemDesignFlow />;
}
